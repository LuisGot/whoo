{
  "name": "whoo",
  "version": "0.1.0",
  "description": "CLI to access your WHOOP health data from your terminal",
  "type": "module",
  "bin": {
    "whoo": "./bin/whoo.js"
  },
  "files": [
    "bin",
    "dist/whoo.js",
    "README.md"
  ],
  "scripts": {
    "start": "bun run src/index.ts",
    "build": "mkdir -p dist && bun build src/index.ts --target=node --format=esm --outfile dist/whoo.js",
    "prepublishOnly": "bun run typecheck && bun run test && bun run build",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.2.1",
    "conf": "^15.1.0",
    "open": "^11.0.0"
  }
}
